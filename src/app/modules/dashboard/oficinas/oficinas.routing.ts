import { Routes } from '@angular/router';
import { IndexOfficeComponent } from './index/index.component';

export const officeRoutes: Routes = [
	{ path: 'oficinas', component: IndexOfficeComponent }
];
